---
id: id3-tl5
title: If no extension is given on the wildcard (i.e., `*` instead of `*.mp3`) determine the file type and either ignore or process accordingly.
status: open
type: feature
priority: 2
tags: []
created: 2026-03-07
updated: 2026-03-07
closed_reason: ''
dependencies: []
description: |
  This means that directories with several file types (e.g., .mp3 and .flac) can be processed with a single command.
---

# If no extension is given on the wildcard (i.e., `*` instead of `*.mp3`) determine the file type and either ignore or process accordingly.

This means that directories with several file types (e.g., .mp3 and .flac) can be processed with a single command.
