[workspace]

members = [
    "id3cli-gen",
    "id3show",
    "id3tag",
    "common",
]

[profile.release]
strip = true

# generated by 'cargo dist init'
[profile.dist]
inherits = "release"
debug = true
split-debuginfo = "packed"

